export const meta = {
  name: 'FormikCheckbox',
  route: '/formik/FormikCheckbox',
  menu: 'Formik',
}

import { useFormik, FormikProvider } from 'formik'
import { FormikCheckbox } from 'practical-react-components-formik'
import { Typohgraphy, SpaceBlock } from 'practical-react-components-core'

# FormikCheckbox

A formik component that wraps a Checkbox component.

## Demo

```typescript type="demo"
<FormikDemo initialValues={{ enabled: false, colors: ['green'] }}>
  <SpaceBlock variant={16} />
  <Typography variant="explanatory-text">Single (true/false)</Typography>
  <SpaceBlock variant={8} />
  <FormikCheckbox label="Enable flux capacitor" name="enabled" />
  <SpaceBlock variant={16} />

  <Typography variant="explanatory-text">Multiple (array)</Typography>
  <SpaceBlock variant={8} />
  <FormikCheckbox label="Red" name="colors" value="red" />
  <FormikCheckbox label="Green" name="colors" value="green" />
  <FormikCheckbox label="Blue" name="colors" value="blue" />
</FormikDemo>
```

## Usage in code

```typescript
import React, { useMemo, useCallback } from 'react'
import { useFormik, FormikProvider } from 'formik'
import { FormikCheckbox } from 'practical-react-components-formik'
import * as yup from 'yup'

const MyComponent = ({ enabled }) => {
  const validationSchema = useMemo(
    () =>
      yup.object().shape({
        enabled: yup.boolean(),
      }),
    []
  )

  const initialValues = useMemo(
    () => ({
      enabled,
    }),
    // Initial values that should not change during
    // re-renders as formik handles state by itself
    // eslint-disable-next-line react-hooks/exhaustive-deps
    []
  )

  const onSubmit = useCallback((values: FormikValues) => {
    /* Save data here */
  }, [])

  const formik = useFormik({
    initialValues,
    validationSchema,
    onSubmit,
  })

  return (
    <FormikProvider value={formik}>
      <FormikCheckbox label="Enabled" name="enabled" />
    </FormikProvider>
  )
}
```

## Props

<Props of="FormikCheckbox" />
